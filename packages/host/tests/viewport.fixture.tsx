/** @jsxImportSource @argui/solid */

/** Compile-time Solid fixture for the generated native viewport contract. */
export function solidViewportFixture() {
  return <gpuCanvas canvas_id={1} width="100%" height={240} resolution_scale={1.5} alt="Preview" />
}
